import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;

/**
 *  문제 설명>
 *  대문자와 소문자가 같이 존재하는 문자열을 입력받아 대문자는 소문자로 소문자는 대문자로 변환하여 출력하는 프로그램을 작성하세요.
 *
 *  입력>
 *  첫 줄에 문자열이 입력된다. 문자열의 길이는 100을 넘지 않습니다.
 *  문자열은 영어 알파벳으로만 구성되어 있습니다.
 *
 *  출력>
 *  첫 줄에 대문자는 소문자로, 소문자는 대문자로 변환된 문자열을 출력합니다.
 */

public class Main {
    public String solution(String str) {
        String result="";

        for (char c : str.toCharArray()) {
            // 아스키 코드를 이용 : 대문자는 65-90, 소문자는 97-122, 둘의 차이는 32
            // 변환 후 casting 필요!
            if(97 <= c && c <= 122) result += (char)(c - 32);
            else result += (char)(c + 32);
//            if(Character.isUpperCase(c)) result += Character.toLowerCase(c);
//            else result += Character.toUpperCase(c);
        }
        return result;
    }

    public static void main(String[] args) throws IOException {
        Main T = new Main();
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));

        String s = br.readLine();

        String str = T.solution(s);

        System.out.println(str);
    }
}
